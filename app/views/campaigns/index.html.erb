<p id="notice"><%= notice %></p>

<h1>Campaigns</h1>

<table>
  <thead>
    <tr>
      <th>Campaign</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @campaigns.each do |campaign| %>
      <tr>
        <td><%= campaign.campaign %></td>
        <td><%= link_to 'Show', campaign %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
